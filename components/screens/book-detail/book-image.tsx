import React, { FunctionComponent } from 'react';
import { Text, View } from 'react-native';

const BookImages: FunctionComponent = () => {
  return (
    <View className="h-60 w-full items-center justify-center rounded-lg bg-slate-500">
      <Text className="text-foreground">BookImages</Text>
    </View>
  );
};

export default BookImages;
