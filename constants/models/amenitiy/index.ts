export interface AmentityResponse {
  id: string;
  name: string;
  description: string;
  iconUrl: string;
  createdAt: Date;
}

export interface AmenityDetailResponse {
  id: string;
  name: string;
  description: string;
  iconUrl: string;
  createdAt: Date;
}
