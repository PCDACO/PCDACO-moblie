export enum Role {
  Driver = 'Driver',
  Owner = 'Owner',
}

export enum FinancialReportStatus {
  Pending = 'Pending',
  Completed = 'Completed',
  Rejected = 'Rejected',
}

export enum CarStatus {
  Available = 'Available',
  Rented = 'Rented',
  Inactive = 'Inactive',
  Pending = 'Pending',
}
